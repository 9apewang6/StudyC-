#ifndef _PUBLICDEF_H_
#define _PUBLICDEF_H_
#include <iostream>
#include <thread>
#include <mutex>
#include <string>
#include <map>
#include <list>
#include <vector>
#include <stack>
#include <queue>
#include <functional>
#include <algorithm>
#include <WinSock2.h>
#include <time.h>


// thread
//  operation  :  
/*
thread
~thread
get_id :获取线程 id , unsigned int 类型
detach
joinable: 线程是否关联 ， id是否为0 ，返回true ，才可以join
join

native_handle
operator= // 已经删除
swap

data:
id
native_handle_type

static_member functions
hardware_concurrency
non-member overloads:
swap(thread);


multiple threading
atomic
conditional_variable  // wait , notify   
future
mutex
thread
*/
#endif //_PUBLICDEF_H_
